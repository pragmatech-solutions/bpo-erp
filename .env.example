# MongoDB connection string. Required — the app throws at startup if unset.
MONGODB_URI=

# Secret key used to sign session JWTs (auth/backend/login, common/backend/get-current-user, proxy.ts).
# Falls back to an insecure default in development if unset — always set this in shared/deployed environments.
JWT_CREATION_KEY=
